apiVersion: ec2.aws.crossplane.io/v1alpha2
kind: InternetGateway
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    tags:
      Name: main
    vpcIdRef:
      name: example
  providerConfigRef:
    name: example