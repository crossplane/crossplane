apiVersion: meta.pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: getting-started-with-gcp
  annotations:
    guide: quickstart
    provider: gcp
spec:
  crossplane:
    version: ">=v0.14.0-0"
  dependsOn:
    - provider: crossplane/provider-gcp
      version: ">=v0.13.0-0"
