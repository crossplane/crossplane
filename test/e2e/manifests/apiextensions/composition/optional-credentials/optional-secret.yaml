apiVersion: v1
kind: Secret
metadata:
  namespace: crossplane-system
  name: optional-secret-does-not-exist
# This secret is created later in the test to verify that optional
# credentials are passed to functions when they become available.
data:
  key: dmFsdWU=  # base64 encoded "value"
