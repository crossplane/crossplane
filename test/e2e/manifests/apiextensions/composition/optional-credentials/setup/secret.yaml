apiVersion: v1
kind: Secret
metadata:
  namespace: crossplane-system
  name: required-secret-exists
# We don't actually use the data, we just want to make sure the required
# credential works alongside the optional one.
data: {}
