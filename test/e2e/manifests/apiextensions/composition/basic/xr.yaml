apiVersion: example.org/v1alpha1
kind: Test
metadata:
  namespace: default
  name: basic-xr
spec:
  coolField: "I'm cool!"
  crossplane:
    compositionRef:
      name: basic-composition
    writeConnectionSecretToRef:
      name: basic-secret
