apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: crossplane-e2e-depends-on-provider-nop
spec:
  # NOTE(ezgidemirel): This package is manually built and pushed to the registry.
  package: xpkg.crossplane.io/crossplane/e2e-depends-on-provider-nop:v0.3.1