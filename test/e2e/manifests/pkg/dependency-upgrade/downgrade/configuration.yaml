apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: configuration-downgrade
spec:
  # NOTE(ezgidemirel): This package is manually built and pushed to the registry.
  package: xpkg.upbound.io/crossplane/e2e-nested-configuration:v0.2.0-d