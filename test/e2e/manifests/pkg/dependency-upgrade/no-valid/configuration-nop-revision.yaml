# We don't install this directly, but we do use it to ensure the configuration is completely deleted.
# Please note, its name will change if provider package contents changes.
apiVersion: pkg.crossplane.io/v1
kind: ConfigurationRevision
metadata:
  name: crossplane-e2e-depends-on-provider-nop-abde574a1e86
spec: {}
