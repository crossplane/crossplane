apiVersion: diff.example.org/v1alpha1
kind: XNopResource
metadata:
  name: existing-resource
spec:
  coolField: "I'm existing!"
  parameters:
    config:
      setting1: original
      setting2: original
