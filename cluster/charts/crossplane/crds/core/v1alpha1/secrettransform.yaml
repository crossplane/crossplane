apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: secrettransforms.core.crossplane.io
spec:
  group: core.crossplane.io
  names:
    kind: SecretTransform
    plural: secrettransforms
  scope: Namespaced
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        destinationRef:
          description: DestinationReference is the destination secret, written according
            to this SecretTransform. It should not exist; it will be created by Crossplane.
          type: object
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        sourceRef:
          description: SourceReference is the source secret, presumed to be an existing
            standard Crossplane connection secret.
          type: object
        template:
          description: DataTemplate is a map of keys to value templates. Keys correspond
            to the keys of the DestinationReference's Data map. Templates are Go templates,
            executed against the DestinationReference's data map.
          type: object
      required:
      - sourceRef
      - destinationRef
      - template
  version: v1alpha1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
