apiVersion: network.aws.crossplane.io/v1alpha2
kind: SecurityGroup
metadata:
  name: my-controlplane-sg
spec:
  vpcIdRef:
    name: my-vpc
  groupName: my-controlplane-sg
  description: Cluster communication with worker nodes
  reclaimPolicy: Delete
  providerRef:
    name: aws-provider
    namespace: crossplane-system
